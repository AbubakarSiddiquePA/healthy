import 'package:flutter/material.dart';

class AprilScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("April"),
      ),
      body: Center(
        child: Text("April Screen"),
      ),
    );
  }
}
